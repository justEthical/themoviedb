import 'package:flutter/material.dart';

class StaticSearchBox extends StatelessWidget {
  const StaticSearchBox({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}